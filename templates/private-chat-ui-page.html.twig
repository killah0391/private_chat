<div class="chat-ui-container ui-container">
	<aside class="chat-ui-sidebar ui-sidebar">
		{# Das <ul> umschließt die Liste. Die ID kommt auf das <ul>. #}
		<ul class="chat-list" id="chat-list-wrapper">
			{% for item in chat_list %}
				{{ item }}
			{% endfor %}
		</ul>
	</aside>

	<main class="chat-ui-content ui-content" id="chat-content-wrapper">
		{{ chat_content }}
	</main>
</div>
